[pytest]
testpaths = tests
addopts = --cov=src --cov-fail-under=70 -m "not integration"
markers =
    integration: marks tests as end-to-end integration tests that require GEMINI_API_KEY (deselect with -m "not integration")
